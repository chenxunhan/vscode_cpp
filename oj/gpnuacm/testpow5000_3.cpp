#include <bits/stdc++.h>
#include <deque>
using namespace std;
unsigned long long N = 5000 * 5000 ;
long long m = LONG_MAX;
string s;
int main() {
    double t0 = time(NULL);
    while (N--) {
	s+="a";   	
    }
    cout << time(NULL) - t0;
    return 0;
}
